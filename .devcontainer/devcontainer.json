// See https://containers.dev/implementors/json_reference/ for configuration reference
{
	"name": "Louis Frontend Node.js project",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"forwardPorts": [3000],
	"postCreateCommand": "npm install; cd app/frontend; npm install",
  "remoteUser": "node"
}
